<Window x:Class="projectover.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:projectover"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="ConsultSchool" Height="800" Width="1400"
        WindowStyle="None"
        AllowsTransparency="True" 
        Background="Transparent">
    
    <Window.Resources>
        <Style x:Key="CircleButton" TargetType="Button">
            <Setter Property="Width" Value="30"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="15"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border CornerRadius="20" Background="White" BorderBrush="LightGray" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <!-- แถบด้านบน -->
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- ✅ Custom Title Bar -->
            <Border Grid.Row="0"
                    CornerRadius="20,20,0,0"
                    Background="#979493"
                    BorderThickness="0"
                    MouseDown="Window_MouseDown">

                <Grid>
                    <TextBlock Text="ConsultSchool"
                               VerticalAlignment="Center"
                               Margin="10,0,0,0"
                               FontWeight="Bold"
                               Foreground="#444"/>

                    <!-- ปุ่มระบบ -->
                    <StackPanel Orientation="Horizontal"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center"
                                Margin="0,0,10,0">
                        <Button Content="—" Style="{StaticResource CircleButton}" Background="#f0e6e6" BorderBrush="#f0e6e6" Click="Minimize_Click" Margin="0,0,5,0"/>
                        <Button Content="☐" Style="{StaticResource CircleButton}" Background="#f0e6e6" BorderBrush="#f0e6e6" Click="Maximize_Click" Margin="0,0,5,0"/>
                        <Button Content="✕" Style="{StaticResource CircleButton}" Background="#e81123" Foreground="White"
                                BorderThickness="0" Click="Close_Click" Margin="0,0,5,0"/>
                    </StackPanel>
                </Grid>
            </Border>
                <!-- เนื้อหาหลัก -->
            <Frame x:Name="MainFrame" Grid.Row="1" NavigationUIVisibility="Hidden"/>
        </Grid>
    </Border>
</Window>
